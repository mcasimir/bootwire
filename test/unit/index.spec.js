const boot = require('../../lib');

describe('bootwire', function() {
  describe('boot', function() {
    it('passes initialContext', function() {
      boot(function() {

      });
    });
  });
});
